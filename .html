<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deep Space Command</title>
    <style>
        /* Base Cosmic Styling */
        :root {
            --space-bg: #03030d;
            --nebula-glow: #6366f1;
            --alien-neon: #06b6d4;
            --starlight: #ffffff;
            --panel-bg: rgba(13, 13, 35, 0.65);
            --panel-border: rgba(99, 102, 241, 0.25);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: var(--space-bg);
            color: #e2e8f0;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            overflow-x: hidden;
            min-height: 100vh;
            position: relative;
        }

        /* Twinkling Starfield Backdrop */
        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background-image: 
                radial-gradient(1px 1px at 20px 30px, var(--starlight), rgba(0,0,0,0)),
                radial-gradient(1px 1px at 75px 130px, var(--starlight), rgba(0,0,0,0)),
                radial-gradient(2px 2px at 150px 60px, var(--starlight), rgba(0,0,0,0)),
                radial-gradient(2px 2px at 220px 190px, rgba(255,255,255,0.4), rgba(0,0,0,0)),
                radial-gradient(1.5px 1.5px at 300px 100px, var(--starlight), rgba(0,0,0,0));
            background-size: 350px 350px;
            opacity: 0.85;
        }

        /* Floating Nebula Core */
        .nebula {
            position: fixed;
            top: -20%;
            right: -10%;
            width: 60vw;
            height: 60vw;
            background: radial-gradient(circle, rgba(99, 102, 241, 0.15) 0%, rgba(6, 182, 212, 0.05) 50%, rgba(0,0,0,0) 70%);
            z-index: -1;
            filter: blur(40px);
            animation: floatNebula 20s ease-in-out infinite alternate;
        }

        @keyframes floatNebula {
            0% { transform: translate(0, 0) scale(1); }
            100% { transform: translate(-5%, 10%) scale(1.1); }
        }

        /* Navigation Header */
        header {
            background: rgba(3, 3, 13, 0.8);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--panel-border);
            padding: 1.25rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            letter-spacing: 2px;
            background: linear-gradient(45deg, var(--alien-neon), var(--nebula-glow));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-transform: uppercase;
        }

        nav a {
            color: #94a3b8;
            text-decoration: none;
            margin-left: 1.5rem;
            font-weight: 500;
            font-size: 0.9rem;
            transition: color 0.3s, text-shadow 0.3s;
        }

        nav a:hover {
            color: var(--alien-neon);
            text-shadow: 0 0 8px var(--alien-neon);
        }

        /* Hero Banner */
        .hero {
            max-width: 1200px;
            margin: 4rem auto 2rem auto;
            padding: 0 2rem;
            text-align: center;
        }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 900;
            letter-spacing: -1px;
            margin-bottom: 1rem;
            color: #ffffff;
        }

        .hero p {
            font-size: 1.2rem;
            color: #94a3b8;
            max-width: 600px;
            margin: 0 auto 2rem auto;
            line-height: 1.6;
        }

        /* Futuristic UI Grid Panels */
        .grid-container {
            max-width: 1200px;
            margin: 0 auto 4rem auto;
            padding: 0 2rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .panel {
            background: var(--panel-bg);
            border: 1px solid var(--panel-border);
            border-radius: 16px;
            padding: 2rem;
            backdrop-filter: blur(8px);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
            transition: transform 0.3s ease, border-color 0.3s ease;
        }

        .panel:hover {
            transform: translateY(-5px);
            border-color: var(--alien-neon);
            box-shadow: 0 0 20px rgba(6, 182, 212, 0.15);
        }

        .panel h3 {
            font-size: 1.3rem;
            color: #ffffff;
            margin-bottom: 0.75rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .panel p {
            color: #94a3b8;
            font-size: 0.95rem;
            line-height: 1.6;
            margin-bottom: 1.5rem;
        }

        /* Glow Button UI */
        .btn {
            background: transparent;
            border: 1px solid var(--nebula-glow);
            color: #ffffff;
            padding: 0.6rem 1.2rem;
            border-radius: 8px;
            font-size: 0.85rem;
            font-weight: 600;
            cursor: pointer;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
            text-transform: uppercase;
        }

        .btn:hover {
            background: var(--nebula-glow);
            box-shadow: 0 0 15px var(--nebula-glow);
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 2rem;
            font-size: 0.8rem;
            color: #475569;
            border-top: 1px solid rgba(255,255,255,0.05);
            background: rgba(3, 3, 13, 0.9);
        }
    </style>
</head>
<body>

    <!-- Nebula Glow Layer -->
    <div class="nebula"></div>

    <!-- Top Navigation Bar -->
    <header>
        <div class="logo">Orbit.OS</div>
        <nav>
            <a href="#">Telemetry</a>
            <a href="#">Star Systems</a>
            <a href="#">Galaxies</a>
            <a href="#">Settings</a>
        </nav>
    </header>

    <!-- Hero Matrix Section -->
    <section class="hero">
        <h1>Explore the Void</h1>
        <p>A minimalist HTML ecosystem modeled around modern sci-fi interfaces and deep galactic exploration aesthetics.</p>
    </section>

    <!-- Main Content Panels -->
    <main class="grid-container">
        <div class="panel">
            <h3><span>✦</span> Deep Space Radar</h3>
            <p>Monitor nearby cosmic anomalies, approaching asteroids, and trace transmissions emitting from distant nebulae clusters.</p>
            <button class="btn">Initialize Scan</button>
        </div>

        <div class="panel">
            <h3><span>🪐</span> Planetary Data</h3>
            <p>Log environmental properties, atmospheric pressures, gravitational coefficients, and life habitability factors across mapped exoplanets.</p>
            <button class="btn">View Catalog</button>
        </div>

        <div class="panel">
            <h3><span>🚀</span> Warp Mechanics</h3>
            <p>Configure tachyon drives, calculate spatial hyper-jumps, and manage anti-matter fuel cells efficiently before leaving orbit.</p>
            <button class="btn">Engage Core</button>
        </div>
    </main>

    <!-- System Footer -->
    <footer>
        &copy; 2026 Orbit.OS Core Systems. All telemetry active.
    </footer>

</body>
</html>
